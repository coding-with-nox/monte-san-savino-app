replicaCount: 2
image:
  repository: ghcr.io/your-org/miniatures-contest-frontend
  tag: "latest"
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: false
  className: ""
  hosts:
    - host: app.example.com
      paths:
        - path: /
          pathType: Prefix
