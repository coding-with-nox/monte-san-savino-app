# ---------------------------------------------------------------------------
# docker-compose.debug.yml
#
# Ambiente di sviluppo/debug con hot-reload e ispettore Bun.
#
# Avvio:
#   docker compose -f docker-compose.debug.yml up --build
#
# Differenze rispetto a docker-compose.yml:
#   • backend  : sorgenti montati, hot-reload (bun --hot), ispettore su :9229
#   • frontend : Vite dev server (:5173) con proxy → backend:3000
#   • db-init  : esegue 02-test-data.sh SOLO se la tabella users è vuota
#   • adminer  : GUI leggera per PostgreSQL su :8888
#   • minio    : console MinIO su :9001 (invariata)
# ---------------------------------------------------------------------------
services:

  # -------------------------------------------------------------------------
  # PostgreSQL (identico alla prod, porta esposta per accesso diretto)
  # -------------------------------------------------------------------------
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      TENANT_DB_NAME: ${TENANT_DB_NAME:-tenant_db_1}
    ports:
      - "5432:5432"
    volumes:
      - pgdata_debug:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 3s
      timeout: 3s
      retries: 10

  # -------------------------------------------------------------------------
  # MinIO (object storage — console su :9001)
  # -------------------------------------------------------------------------
  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    volumes:
      - miniodata_debug:/data

  # -------------------------------------------------------------------------
  # db-schema + db-seed: caricamenti separati
  # -------------------------------------------------------------------------
  # One-shot psql client: loads schema into the postgres service database.
  db-schema:
    image: postgres:16
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      TENANT_DB_NAME: ${TENANT_DB_NAME:-tenant_db_1}
      PGHOST: postgres
      PGPORT: 5432
    volumes:
      - ./docker/postgres/01-init-tenant.sh:/scripts/01-init-tenant.sh:ro
    command: >
      bash -c "until pg_isready -h postgres -p 5432 -U $${POSTGRES_USER}; do sleep 1; done;
      bash /scripts/01-init-tenant.sh"
    restart: "no"

  # One-shot psql client: loads seed data into the same postgres service database.
  db-seed:
    image: postgres:16
    depends_on:
      backend:
        condition: service_started
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      TENANT_DB_NAME: ${TENANT_DB_NAME:-tenant_db_1}
      PGHOST: postgres
      PGPORT: 5432
    volumes:
      - ./docker/postgres/02-test-data.sh:/scripts/02-test-data.sh:ro
    command: >
      bash -c "
        until pg_isready -h postgres -p 5432 -U $${POSTGRES_USER}; do sleep 1; done;
        export PGPASSWORD=$${POSTGRES_PASSWORD};
        until psql -h postgres -U $${POSTGRES_USER} -d $${TENANT_DB_NAME} -tAc \"SELECT to_regclass('public.team_mates') IS NOT NULL AND to_regclass('public.award_brackets') IS NOT NULL AND to_regclass('public.judge_completions') IS NOT NULL;\" | grep -q t; do sleep 1; done;
        bash /scripts/02-test-data.sh
      "
    restart: "no"

  # -------------------------------------------------------------------------
  # Backend — hot-reload + inspector Bun
  #   Porta 3000 : API
  #   Porta 9229 : Bun inspector (chrome://inspect o VS Code "Attach")
  # -------------------------------------------------------------------------
  backend:
    image: oven/bun:1.1.29
    working_dir: /app
    depends_on:
      postgres:
        condition: service_healthy
      db-schema:
        condition: service_completed_successfully
      minio:
        condition: service_started
    environment:
      NODE_ENV: development
      JWT_SECRET: ${JWT_SECRET:-debug-secret-change-me}
      PG_HOST: postgres
      PG_PORT: 5432
      PG_DB: ${TENANT_DB_NAME:-tenant_db_1}
      PG_USER: ${POSTGRES_USER:-postgres}
      PG_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
    volumes:
      - ./backend:/app
      # node_modules separato per evitare conflitti host/container
      - backend_modules:/app/node_modules
    ports:
      - "3000:3000"
      - "9229:9229"
    command: >
      sh -c "bun install &&
             bun --hot --inspect=0.0.0.0:9229 src/bootstrap/server.ts"

  # -------------------------------------------------------------------------
  # Frontend — Vite dev server con HMR
  #   Porta 5173 : app React
  #   Il proxy /api → backend:3000 è configurato via VITE_PROXY_TARGET
  # -------------------------------------------------------------------------
  frontend:
    image: node:20-alpine
    working_dir: /app
    depends_on:
      backend:
        condition: service_started
      db-seed:
        condition: service_completed_successfully
    environment:
      # Sovrascrive il target del proxy Vite (vite.config.ts lo legge da process.env)
      VITE_PROXY_TARGET: http://backend:3000
    volumes:
      - ./frontend:/app
      - frontend_modules:/app/node_modules
    ports:
      - "5173:5173"
    command: >
      sh -c "npm ci &&
             npx vite --host 0.0.0.0 --port 5173"

  # -------------------------------------------------------------------------
  # Adminer — GUI leggera per ispezione PostgreSQL
  #   http://localhost:8888
  #   Sistema: PostgreSQL | Server: postgres | Utente/Password dal .env
  # -------------------------------------------------------------------------
  adminer:
    image: adminer:4
    depends_on:
      - postgres
    ports:
      - "8888:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres

volumes:
  pgdata_debug:
  miniodata_debug:
  backend_modules:
  frontend_modules:
